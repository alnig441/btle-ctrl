ul(ng-controller='loginCtrl')
    li.navigate Admin:
        button(ng-click='logout()') log out

table(ng-controller='adminCtrl')
    tr
        td
            button.outer(ng-click='scanDev("scan")') SCAN
        td
            button.outer(ng-click='addDev("add")') ADD
    tr
        td
            button.outer(ng-click='updateDev("update")') UPDATE
        td
            button.outer(ng-click='deleteDev("delete")') REMOVE

table(id='viewPane')
    tr
        td(id='loadPane', ng-include='template.url')


