table(ng-controller='panelViewCtrl')
    tr
        td
            button.outer(ng-click='switch("schedule")') Schedule
        td
            button.outer(ng-click='switch("dim")') Dim

    tr
        td
            button.outer(ng-click='switch("colour")') Colour
        td
            button.outer(ng-click='switch("profiles")') Profiles

