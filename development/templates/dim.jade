div(ng-controller="optionsCtrl", ng-cloak)
    md-toolbar
        div.md-toolbar-tools
            h2 Dim
    md-content(style="margin: 16px; padding:16px")
        div(layout)
            div(flex="10", layout, layout-align="center center")
                span.md-body-1
            md-slider(flex min="0", max="10", ng-model="dim.brightness", aria-label="red", id="dim-slider", class)
            div(flex="20", layout, layout-align="center center")
                input(flex, type="number", ng-model="dim.brightness", aria-label="brightness", aria-controls="dim-slider")
        md-button(ng-click='apply("dim")') Apply
            md-icon done
        md-button(ng-click='dismiss()') Cancel
            md-icon cancel


